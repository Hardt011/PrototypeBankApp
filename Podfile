# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppTesteBanco' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppTesteBanco
  
  pod 'Alamofire', '~> 5.4'
  pod "VMaskTextField"
  pod 'KeychainAccess'
  pod "CircleLoading"
  
  
  end
